package top.potmot.core.database.generate.identifier

enum class IdentifierType {
    DEFAULT,
    TABLE_NAME,
    COLUMN_NAME,
    INDEX_NAME,
    CONSTRAINT_NAME,
    PRIMARY_KEY_NAME,
    FOREIGN_KEY_NAME
}
