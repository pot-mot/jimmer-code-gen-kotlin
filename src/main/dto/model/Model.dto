export top.potmot.entity.model.Model

input ModelInsertInput {
    #allScalars
    -id
}

input ModelUpdateInput {
    #allScalars
    id!
}

ModelView {
    #allScalars
}

ModelNoJsonView {
    #allScalars
    -jsonData
}

specification ModelSpec {
    like/i(name)
    like/i(description)
    ge(createdTime)
    le(createdTime)
    ge(modifiedTime)
    le(modifiedTime)
    databaseType
}
