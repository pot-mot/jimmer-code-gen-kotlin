export top.potmot.entity.typeMapping.TsType

TsTypeView {
    #allScalars
    -orderKey
    jvmToTsMappingRules {
        #allScalars
        -orderKey
    }
}

input TsTypeInput {
    #allScalars
    -orderKey
    jvmToTsMappingRules {
        #allScalars
        -orderKey
    }
}
