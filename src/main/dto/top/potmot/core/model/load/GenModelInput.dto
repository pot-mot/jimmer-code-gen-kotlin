export top.potmot.entity.GenModel

input GenModelInput {
    #allScalars(this)

    subGroups {
        #allScalars(this)
        -id
    }

    enums {
        #allScalars(this)
        -id
        subGroup {
            name
        }

        items {
            #allScalars(this)
            -id
        }
    }
}