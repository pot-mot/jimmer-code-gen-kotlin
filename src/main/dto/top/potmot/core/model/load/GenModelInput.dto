export top.potmot.entity.GenModel

input GenModelInput {
    #allScalars(this)

    subGroups {
        #allScalars(this)
        -id
    }

    enums {
        #allScalars(this)
        -id

        items {
            #allScalars(this)
            -id
        }
    }
}