export top.potmot.entity.GenEntity

GenEntityGenerateFileFillView {
    id
    name

    table {
        id
        name
    }
}

GenEntityGenerateView {
    #allScalars

    table {
        id
        name
        type
    }

    superEntities {
        name
    }

    properties {
        #allScalars
        id(entity)

        -keyGroup
        keyGroups

        column {
            id
            name
        }

        typeTable {
            id

            entity {
                id
                packagePath
                name
                comment
            }
        }

        enum {
            id
            packagePath
            name
            comment
        }
    }
}

