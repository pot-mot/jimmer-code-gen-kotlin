export top.potmot.entity.GenModel

GenConfig {
    #allScalars(GenModel)
    -id
    -name
    -graphData
    -remark
}

input GenConfigProperties {
    #allScalars(GenModel)?
    -id
    -name
    -graphData
    -remark
}
