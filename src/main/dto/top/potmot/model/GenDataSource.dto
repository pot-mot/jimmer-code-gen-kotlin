GenDataSourceView {
    #allScalars
    -username
    -password
}

GenDataSourceSchemasView: GenDataSourceView {
    schemas {
        #allScalars
        id(dataSource)
    }
}

inputOnly GenDataSourceInput {
    #allScalars
    -id
    -createdTime
    -modifiedTime
}