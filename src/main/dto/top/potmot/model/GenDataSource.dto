GenDataSourceView {
    #allScalars
    -username
    -password
}

GenDataSourceTemplateView {
    name
    host
    port
    urlSuffix
    username
    remark
}

GenDataSourceSchemasView {
    #allScalars
    -username
    -password

    schemas {
        #allScalars
        id(dataSource)
    }
}

input GenDataSourceInput {
    #allScalars(GenDataSource)
    -id
}
