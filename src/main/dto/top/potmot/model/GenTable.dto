GenTableCommonView {
    #allScalars
    id(dataSource)
}

GenTableColumnsView {
    #allScalars
    id(dataSource)

    columns {
        #allScalars
    }
}

input GenTableCommonInput {
    #allScalars
    id(dataSource)
    -createdTime
    -modifiedTime
}

input GenTableColumnsInput {
    #allScalars
    id(dataSource)
    -createdTime
    -modifiedTime

    columns {
        #allScalars
    }
}
