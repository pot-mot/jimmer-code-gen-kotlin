export top.potmot.entity.GenModel

input GenModelInput {
    #allScalars(this)

    enums {
        #allScalars(this)
        -id
        id(model)

        items {
            #allScalars(this)
            -id
        }
    }
}