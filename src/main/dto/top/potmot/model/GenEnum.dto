GenEnumView {
    #allScalars
    id(model)
}

GenEnumItemsView {
    #allScalars
    id(model)

    items {
        #allScalars
    }
}

input GenEnumItemsInput {
    #allScalars(GenEnum)
    id(model)

    items {
        #allScalars(GenEnumItem)
    }
}
