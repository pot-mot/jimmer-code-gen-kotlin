GenEnumView {
    #allScalars
    id(genPackage)
}

GenEnumItemsView {
    #allScalars
    id(genPackage)

    items {
        #allScalars
    }
}

input GenEnumItemsInput {
    id(genPackage)
    name
    comment

    items {
        name
        value
    }
}
