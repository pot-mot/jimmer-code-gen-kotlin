GenEnumView {
    #allScalars
    id(genPackage)
}

GenEnumItemsView {
    #allScalars
    id(genPackage)

    items {
        #allScalars
    }
}

input GenEnumItemsInput {
    #allScalars
    id(genPackage)
    -createdTime
    -modifiedTime

    items {
        #allScalars
        -createdTime
        -modifiedTime
    }
}
