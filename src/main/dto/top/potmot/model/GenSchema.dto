GenSchemaView {
    #allScalars
    id(dataSource)
}

GenSchemaPreview {
    #allScalars(GenSchema)
    -id
    id(dataSource)
}

input GenSchemaInput {
    #allScalars(GenSchema)
    -id
    dataSourceId
}
