GenPackageTreeView {
     #allScalars
     id(parent)

     children {
        #allScalars
     }*

     entities {
        id
        name
        id(table)
     }

     enums {
        id
        name
     }
}

inputOnly GenPackageInput {
    id
    id(parent)
    name
    orderKey?
}
