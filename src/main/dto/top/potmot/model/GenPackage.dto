GenPackageTreeView {
     #allScalars
     id(parent)

    entities {
        id
        name
        id(table)
    }

    enums {
        id
        name
    }

    children {
        #allScalars

        entities {
            id
            name
            id(table)
        }

        enums {
            id
            name
        }
    }*
}

inputOnly GenPackageInput {
    id
    id(parent)
    name
    orderKey?
}
