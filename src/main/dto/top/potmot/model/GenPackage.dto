GenPackageTreeView {
     #allScalars
     id(parent)

    entities {
        id
        name
        comment
        id(table)
    }

    enums {
        id
        name
        comment
    }

    children {
        #allScalars

        entities {
            id
            name
            id(table)
        }

        enums {
            id
            name
        }
    }*
}

inputOnly GenPackageInput {
    id
    id(parent)
    name
    orderKey?
}
