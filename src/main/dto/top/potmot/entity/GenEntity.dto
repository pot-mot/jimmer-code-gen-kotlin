GenEntityCommonView {
    #allScalars
    id(table)
}

input GenEntityInput {
    #allScalars(this)
    -id
    id(model)
    id(table)
    id(superEntities) as superEntityIds

    properties {
        #allScalars(this)
        -id
        id(column)
        id(typeTable)
        id(enum)
    }
}
