input GenEntityInput {
    #allScalars(this)
    -id
    id(model)
    id(table)
    id(superEntities) as superEntityIds

    properties {
        #allScalars(this)
        id(column)
        id(typeTable)
        id(enum)
    }
}