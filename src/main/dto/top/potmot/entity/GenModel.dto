GenModelSimpleView {
    #allScalars
    -graphData
}

GenModelView {
    #allScalars

    enums {
        #allScalars(this)
        -id
        id(model)

        items {
            #allScalars(this)
            -id
        }
    }
}

input GenModelInput {
    #allScalars(this)

    enums {
        #allScalars(this)
        -id
        id(model)

        items {
            #allScalars(this)
            -id
        }
    }
}
