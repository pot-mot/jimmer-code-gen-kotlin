GenModelSimpleView {
    #allScalars
    -graphData
}

GenModelView {
    #allScalars

    enums {
        #allScalars(GenEnum)
        -id
        id(model)

        items {
            #allScalars(GenEnumItem)
            -id
        }
    }
}

input GenModelInput {
    #allScalars(GenModel)

    enums {
        #allScalars(GenEnum)
        -id
        id(model)

        items {
            #allScalars(GenEnumItem)
            -id
        }
    }
}
