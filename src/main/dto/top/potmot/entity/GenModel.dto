GenModelSimpleView {
    #allScalars
    -graphData
}

GenModelView {
    #allScalars

    subGroups {
        #allScalars(this)
        -id
    }

    enums {
        #allScalars(this)
        -id
        subGroup {
            name
        }

        items {
            #allScalars(this)
            -id
        }
    }
}
