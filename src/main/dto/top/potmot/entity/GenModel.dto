GenModelSimpleView {
    #allScalars
    -graphData
}

GenModelView {
    #allScalars

    subGroups {
        #allScalars(this)
        -id
    }

    enums {
        #allScalars(this)
        -id

        items {
            #allScalars(this)
            -id
        }
    }
}
